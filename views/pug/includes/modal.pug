div.modal.fade(id="deleteModal", tabindex="-1", role="dialog", aria-labelledby="exampleModalLabel", aria-hidden="true")
  div.modal-dialog(role="document")
    div.modal-content
      div.modal-header
        h5.modal-title(id="exampleModalLabel") Delete issue
        button(type="button", data-dismiss="modal", aria-label="Close").close
          span(aria-hidden="true") &times;
      div.modal-body Do you really want to delete that issue?
      div.modal-footer
        button(type="button", data-dismiss="modal").btn.btn-secondary Close
        form(action='', method='POST', id='deleteForm').m-0
          button(type="submit").btn.btn-danger Delete